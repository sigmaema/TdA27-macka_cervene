exposedPort: 80
containers:
  - name: web
    image: registry.tourde.cloud/macka_cervene/web
    port: 3001
    env:
      PORT: "3001"
      VITE_API_URL: "/api"
  - name: server
    image: registry.tourde.cloud/macka_cervene/server
    port: 8080
    env:
      DATABASE_URL: mysql://tda_user:strongPassword%3F@127.0.0.1:3306/product
      PORT: "8080"
  - name: mysql
    image: mysql
    tag: "8.4"
    env:
      MYSQL_DATABASE: product
      MYSQL_ROOT_PASSWORD: password
    port: 3306
  - name: caddy
    image: registry.tourde.cloud/macka_cervene/caddy
    port: 80
build:
  - name: web
    context: ./frontend
    dockerfile: ./frontend/Dockerfile
    args:
      VITE_API_URL: "/api"
  - name: server
    context: ./backend
    dockerfile: ./backend/docker/Dockerfile
  - name: caddy
    context: ./caddy
    dockerfile: ./caddy/Dockerfile
